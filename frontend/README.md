Car Rental Frontend (Next.js + Chakra UI)

Quick start:

1. Install dependencies

```bash
cd frontend
npm install
```

2. Run dev server

```bash
npm run dev
```

The frontend runs on `http://localhost:3000` and talks to the existing backend API (default `http://localhost:5000`).
